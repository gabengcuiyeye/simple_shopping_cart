doctype html
html(lang="en")
    head
        title= 'index'
    body
        div(id='app')
            app
    script.
      list_data= !{init_list_data};
      cart_data= !{init_cart_data};
    script(src='/index.js')
